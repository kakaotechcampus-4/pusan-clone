# 📢 정답 코드 업데이트 안내 — Week 5 버그 수정 2건 + Week 4 RAG 비용 개선

안녕하세요. 정답 코드에 버그 수정이 반영됐습니다.

**한 줄 요약**

| 주차 | 내용 | 내 코드 수정 필요? |
|---|---|---|
| Week 1 · 2 · 3 · 6 | 변경 없음 | ❌ |
| Week 4 | 설명·프롬프트 문구만 (동작은 `fixed/`에서 개선) | ❌ |
| **Week 5** | **`collect_member_schedules` 로직 변경** | ✅ |

**이미 Week 5를 구현하신 분은 [4. 적용 가이드](#4-이미-week-5를-구현하신-분들께--적용-가이드)를 보고 본인 코드에 직접 반영해주세요.** 코드를 그대로 옮겨 쓸 수 있도록 전부 실었습니다.

---

## 1. Week 5 버그 ① — 이미 잡아둔 그룹 일정이 "빈 시간"으로 추천됨

### 증상

일정 조율을 하면 **이미 회의가 잡혀 있는 시간이 공통 가능 시간 후보로** 나왔습니다. 그 rows를 그대로 쓰는 Week 6 `find_common_available_slots`까지 영향을 받았습니다.

> 예) 하린과 잡아둔 회의가 있는데 민준과 일정을 조율하면, 그 회의 시간이 "비어 있는 시간"으로 취급됩니다.

### 원인

저장된 내 일정을 `kind="personal_schedule"`로만 읽고 있었습니다.

```python
# 문제가 된 코드
db_schedules = AppSQLiteStore(CONFIG.app_db_path).list_schedules(limit=200, kind="personal_schedule")
```

그룹 일정은 저장할 때 **참석자별 공유 row로 자동 동기화**됩니다(`fixed/external_mcp.py`의 `sync_group_schedule_to_shared`). 그런데 이 함수는 공유 row를 **참석자 이름으로만** 만들고 `"나"` row는 만들지 않습니다.

그래서 이런 사각지대가 생깁니다.

| 경로 | 하린과의 그룹 회의를 찾는가? (민준과 조율 중일 때) |
|---|---|
| 앱 DB (`list_schedules`) | ❌ `kind` 필터에 걸려 제외 |
| 공유 저장소 (외부 조회) | ❌ 조회 대상이 `["민준"]`뿐, 하린 row는 안 봄 |

**양쪽 어디에서도 안 잡혀 내 바쁜 시간에서 통째로 빠졌습니다.**

### 수정

`kind` 필터를 없앴습니다. `schedules` 테이블의 row는 개인이든 그룹이든 **owner가 '나'인 내 일정**이고, 둘 다 그 시간에 내가 바쁘다는 근거이기 때문입니다.

---

## 2. Week 5 버그 ② — 같은 일정이 두 번 표시됨

### 증상

`member_names`에 `"나"`가 들어오면 같은 일정이 rows에 두 번 나왔습니다.

### 원인

내 일정은 저장될 때 공유 저장소에도 `"나"` 이름으로 동기화됩니다. 그래서 `"나"`를 조회 대상에 넣으면 **같은 일정이 앱 DB 경로와 공유 저장소 경로 두 갈래로** 들어옵니다.

문제는 **두 경로가 같은 일정을 서로 다르게 다듬는다**는 점입니다. 값을 그대로 비교하면 중복이 안 걸러집니다.

| 필드 | 앱 DB 경로 | 공유 저장소 경로 |
|---|---|---|
| 제목 | 원문 그대로 | 소괄호 제거 + 연속 공백을 하나로 |
| `end_time` | `"미정"` → `"18:00"`으로 치환 | `"미정"` 그대로 |
| `start_time` | 비어 있으면 빈 문자열 | 비어 있으면 `"미정"` |

즉 `"팀 회의 (온라인)"` 하나를 저장해도 rows에는 `"팀 회의 (온라인)"`과 `"팀 회의"`가 각각 남습니다.

### 수정

**`(member_name, date, start_time, 다듬은 제목)`** 을 비교 키로 삼아 한 번 걸러냅니다. `end_time`은 앱 DB 경로에서 이미 바뀐 값을 되돌릴 수 없으므로 **키에서 뺐습니다** — 같은 사람이 같은 날 같은 시각에 시작하는 같은 제목의 일정은 하나로 봅니다.

함께 고친 것: `members` 목록에 `"나"`가 두 번 들어가던 문제.

---

## 3. Week 4 — 대화 검색(RAG) 임베딩 비용

이건 `fixed/` 쪽 변경이라 **여러분이 고칠 건 없습니다.** 무슨 일이 있었는지만 공유합니다.

### 문제

대화 1건을 통째로 청크 하나에 담고 있어서, **메시지가 한 건 붙을 때마다 대화 전문을 다시 임베딩**했습니다. 대화가 길어질수록 비용이 급격히 늘었습니다.

### 수정

메시지 8개 단위 window 청킹으로 바꿨습니다. 가득 찬 window는 내용이 더 바뀌지 않으므로 다시 임베딩하지 않습니다. 추가로 **지금 진행 중인 대화는 어차피 검색에서 제외**되므로 임베딩 자체를 건너뜁니다.

실측 결과 (메시지를 하나씩 추가하며 매번 sync, 누적 임베딩 문자 수):

| 대화 길이 | 수정 전 | 수정 후 | 절감 |
|---|---|---|---|
| 20 메시지 | 15,001자 | 5,889자 | 2.5배 |
| 40 메시지 | 58,701자 | 12,925자 | 4.5배 |
| 80 메시지 | 232,501자 | 25,845자 | **9.0배** |

대화 길이가 2배가 되면 예전에는 비용이 4배로 늘었지만 지금은 2배만 늘어납니다. 비용 곡선이 **제곱에서 선형으로** 바뀌었고, 대화가 길수록 절감폭이 계속 커집니다.

### 후속 개선

- **제목으로도 대화를 찾습니다.** window 청킹으로 넘어가면서 대화 제목이 검색 대상에서 빠졌던 것을 되돌렸습니다. "제주도 여행 얘기했던 대화 찾아줘"처럼 주제·제목으로 불러도 찾습니다.
- **검색 결과가 한 대화에 쏠리지 않습니다.** 긴 대화 하나가 결과를 독점하지 않도록 대화당 window 개수를 제한했습니다. 단, 대상 대화가 하나뿐이면 제한 때문에 결과가 줄어들지는 않습니다.

### 첫 실행 시 참고

pull 후 첫 실행에서 **기존 대화가 한 번 전체 재임베딩**됩니다. 청크 저장 방식이 바뀌었기 때문이며, 일회성이고 자동으로 처리됩니다. 이후부터 위 표의 절감 효과가 적용됩니다.

---

## 4. 이미 Week 5를 구현하신 분들께 — 적용 가이드

대상 파일은 **`student_parts/week05_load_kanas_past_conversations.py`** 하나뿐이고, 고칠 곳도 **`collect_member_schedules` 계열**뿐입니다.

| 할 일 | 곳 |
|---|---|
| 한 줄 수정 | (A) `_personal_schedules_for_current_scope()` |
| 한 줄 수정 | (B) `_structured_request_from_schedule_row()` |
| helper 추가 | (C) `_my_schedule_notes()`, (D) `_dedupe_schedule_rows()` |
| 세 줄 수정 | (E) `_collect_member_schedules()` |

(A)~(E) 순서대로 반영하시면 됩니다.

### (A) `kind` 필터 제거 — `_personal_schedules_for_current_scope()`

```python
# BEFORE
db_schedules = AppSQLiteStore(CONFIG.app_db_path).list_schedules(limit=200, kind="personal_schedule")

# AFTER
db_schedules = AppSQLiteStore(CONFIG.app_db_path).list_schedules(limit=200)
```

> `list_schedules`는 `kind`를 넘길 때만 필터를 겁니다. 안 넘기면 개인·그룹이 모두 들어옵니다.

### (B) 일정 종류를 row에서 읽기 — `_structured_request_from_schedule_row()`

`kind`를 `"personal_schedule"`로 하드코딩하지 말고 **row의 `request_kind`** 에서 읽습니다. Week 1 임시 일정 row에는 이 값이 없으니 그때만 개인 일정으로 봅니다. **`kind=` 한 줄만 바꾸면 됩니다.**

```python
# BEFORE
        kind="personal_schedule",

# AFTER
        kind="group_schedule" if row.get("request_kind") == "group_schedule" else "personal_schedule",
```

docstring에 이유를 남겨두시면 좋습니다.

```python
    """앱 일정 row를 Week 2 StructuredRequest 기준으로 읽습니다.

    SQLite row는 `request_kind`로 개인/그룹을 구분합니다. Week 1 임시 일정 row에는
    이 값이 없으므로 개인 일정으로 봅니다.
    """
```

### (C) `notes`를 종류에 따라 구분 — 새 helper

```python
def _my_schedule_notes(request: StructuredRequest) -> str:
    """내 일정 row가 개인 일정인지, 참석자가 있는 그룹 일정인지 설명합니다."""

    if request.kind != "group_schedule":
        return "Nana 개인 일정"
    members = [str(member).strip() for member in (request.members or []) if str(member).strip()]
    return f"Nana 그룹 일정 · 참석자: {', '.join(members)}" if members else "Nana 그룹 일정"
```

`_collect_member_schedules` 안에서 `notes`를 이걸로 바꿉니다.

```python
# BEFORE
"notes": "Nana 개인 일정",

# AFTER
"notes": _my_schedule_notes(request),
```

기대 결과:

- 개인 → `"Nana 개인 일정"`
- 그룹 → `"Nana 그룹 일정 · 참석자: 하린, 민준"` (참석자가 없으면 `"Nana 그룹 일정"`)

### (D) 중복 제거 — 새 helper

파일 상단 import에 `strip_parenthetical_text`를 추가합니다.

```python
from fixed.external_people_store import (
    external_schedule_summary,
    normalize_external_member_names,
    normalize_external_schedule_date_bounds,
    strip_parenthetical_text,   # 추가
)
```

```python
def _dedupe_schedule_rows(rows: list[dict[str, Any]]) -> list[dict[str, Any]]:
    """같은 일정이 앱 DB와 공유 저장소 양쪽에서 들어와도 한 번만 남깁니다.

    앱 DB에 저장된 내 일정은 공유 저장소에도 자동 동기화되므로, member_names에 "나"가
    들어온 호출에서는 같은 일정이 두 경로로 들어옵니다. 앞에 오는 앱 DB row를 남깁니다.

    두 경로가 같은 일정을 서로 다르게 다듬기 때문에 값을 그대로 비교하면 안 됩니다.
      - 공유 저장소는 제목에서 소괄호를 지우고 공백을 하나로 줄입니다. 앱 DB는 원문을 둡니다.
      - 앱 DB 경로만 end_time "미정"을 "18:00"으로 바꿉니다. 그래서 end_time은 키에서 뺍니다.
        같은 사람이 같은 날 같은 시각에 시작하는 같은 제목의 일정은 하나로 봅니다.
      - start_time이 비어 있으면 공유 저장소는 "미정"으로 저장하므로 같은 값으로 맞춥니다.
    """

    deduped: dict[tuple[str, ...], dict[str, Any]] = {}
    for row in rows:
        key = (
            str(row.get("member_name") or "").strip(),
            str(row.get("date") or "").strip(),
            str(row.get("start_time") or "").strip() or "미정",
            strip_parenthetical_text(str(row.get("title") or "")),
        )
        deduped.setdefault(key, row)
    return list(deduped.values())
```

> dict은 넣은 순서를 유지하고 `setdefault`는 이미 있는 키를 덮어쓰지 않으므로, 먼저 들어온 row가 남습니다.

### (E) 마무리 — rows 합치는 부분과 `members`

```python
# BEFORE
rows = [*my_rows, *external_payload.get("rows", [])]
return {
    "ok": True,
    "tool_name": "collect_member_schedules",
    "members": ["나", *normalized_members],
    "rows": rows,
    "schedule_summary": external_schedule_summary(rows),
}

# AFTER
rows = _dedupe_schedule_rows([*my_rows, *external_payload.get("rows", [])])
return {
    "ok": True,
    "tool_name": "collect_member_schedules",
    "members": ["나", *[name for name in normalized_members if name != "나"]],
    "rows": rows,
    "schedule_summary": external_schedule_summary(rows),
}
```

> ⚠️ **`my_rows`를 external rows보다 먼저** 두어야 합니다. `_dedupe_schedule_rows`는 앞에 오는 row를 남기는데, 앱 DB row의 `notes`는 (C)에서 만든 `"Nana 개인 일정"` / `"Nana 그룹 일정 · 참석자: …"`인 반면 공유 저장소 row의 `notes`는 `"앱 개인 일정 자동 동기화"`입니다. 순서를 바꾸면 중복은 사라져도 `notes`가 엉뚱한 값으로 남습니다.

### 반영 후 앱에서 이렇게 동작합니다

**① 그룹 일정이 바쁜 시간에 잡힙니다 (버그 ①)**

1. `"7월 14일 3시에 하린이랑 사전 미팅 잡아줘"` — 하린과 **그룹 일정**을 만듭니다.
2. `"민준이랑 이번 주 일정 맞춰줘"` — 하린이 **빠진** 다른 사람과 조율합니다.
3. 결과 rows에 `member_name: "나"` / `title: "하린과 사전 미팅"` row가 나오고, `notes`는 `"Nana 그룹 일정 · 참석자: 하린"`입니다.

> 수정 전에는 이 row가 아예 없고, 7월 14일 3시가 "가능한 시간"으로 추천됩니다.

**② 중복이 사라집니다 (버그 ②)**

1. `"7월 14일 3시에 팀 회의 (온라인) 잡아줘"` — 제목에 **소괄호를 넣어** 개인 일정을 만듭니다.
2. `"나랑 민준이 일정 정리해줘"` — 조율 대상에 **"나"를 포함**시킵니다.
3. `member_name: "나"` row가 **1건만** 나오고, `members`도 `["나", "민준"]`입니다.

> 수정 전에는 `"팀 회의 (온라인)"`과 `"팀 회의"`가 각각 남아 2건이 되고, `members`에 `"나"`가 두 번 들어갑니다.

### 바뀌지 않은 것

**tool 시그니처, 입력 스키마, 반환 payload 키는 그대로입니다.** `ok` / `tool_name` / `members` / `rows` / `schedule_summary` 구조 안에서 **값만** 달라집니다. 구조를 새로 맞출 필요는 없습니다.

---

## 5. pull 안내

**pull 하기 전에 본인 작업을 먼저 커밋하세요.**

```bash
git add -A && git commit -m "wip: 내 구현 백업"
git pull
```

Week 5 파일은 정답 코드 쪽에서도 바뀌었기 때문에, 같은 부분을 수정하셨다면 충돌이 날 수 있습니다. 그때는 **본인 구현을 살리고 위 (A)~(E)만 반영**하시면 됩니다.

정답 구현 전체를 참고하고 싶으면 충돌을 해결한 뒤 이렇게 볼 수 있습니다.

```bash
git show origin/main:student_parts/week05_load_kanas_past_conversations.py | less
```

질문 있으시면 편하게 남겨주세요.
