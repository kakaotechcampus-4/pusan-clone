# 에이전트 최종 답변 검토 기준

검토 대상은 `.eval-artifacts/routing-answers.json`이다.
추가 eval이나 외부 API를 호출하지 않고 artifact에 저장된 `user`, `history`,
`tool_trace`, `answer`만 읽어 검토한다.

## 검토 방법

각 실행(`runs`)을 다음 순서로 확인한다.

1. `user`와 `history`에서 사용자가 요구한 내용을 파악한다.
2. `tool_trace`의 tool result에서 답변에 사용할 수 있는 사실을 확인한다.
3. `answer`가 질문에 답하면서 확인된 사실과 일치하는지 판정한다.

Tool call의 이름이나 인자만으로는 사실이 확인됐다고 보지 않는다. 실제 tool result와
현재 대화에 나온 내용만 답변의 근거로 인정한다.

## 판정 기준

다음을 모두 만족하면 `PASS`다.

- 사용자 질문에 직접 답한다.
- 답변의 사실이 현재 대화 또는 tool result에 근거한다.
- 사용자가 요구한 핵심 사실을 누락하지 않는다.
- 현재 대화나 tool result와 모순되지 않는다.
- 근거에 없는 일정, 날짜, 시간, 사람 또는 처리 결과를 지어내지 않는다.
- 변경 tool의 성공을 주장했다면 해당 tool result에서 성공이 확인된다.

다음 중 하나가 명확하면 `FAIL`이다.

- 질문의 핵심에 답하지 않는다.
- 필요한 핵심 사실을 누락한다.
- 근거와 모순되는 내용을 말한다.
- 근거에 없는 사실이나 성공 여부를 단정한다.
- 빈 검색 결과를 전체 기록의 부재로 과장한다.

정보가 부족하거나 두 판정 모두 합리적이면 억지로 결정하지 않고 `REVIEW`로 남긴다.

## 허용하는 표현 차이

다음은 의미가 같으면 같은 사실로 본다.

- `2026-07-07`, `7월 7일`
- `10:00`, `오전 10시`
- 조사, 어순, 문체 또는 부가적인 친절한 설명의 차이

특정 키워드가 없다는 이유만으로 실패시키지 않는다.

빈 검색 결과에서는 검색 범위의 한계를 밝히는 답변을 허용한다.

- 허용: `현재 검색어로는 확인하지 못했습니다.`
- 실패: `저장된 기록이 전혀 없습니다.`

## 결과 작성 형식

| case_id | run | 판정 | 근거 |
|---|---:|---|---|
| 예시 | 0 | PASS / FAIL / REVIEW | tool result와 답변을 비교한 이유 |

`behavior_failures`는 tool routing 판정 결과다. 참고 자료로 사용하되, 그 값만으로 최종
답변의 의미 판정을 대신하지 않는다.
